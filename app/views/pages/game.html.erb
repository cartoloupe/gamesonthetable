<div id='user_info' ng-controller='UserController'>

  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <h1>User Information</h1>
      <p>Current user (retrieved with erb) is <%= current_user.email %></p>
      <p>Current user (retrieved with Angular) is {{greeting}}</p>
      <button ng-click='getUser()'>getUser</button>
    </div>
  </div>


  <br>
  <div class="row">
    <div class="board col-md-offset-2">
      <table class="table-bordered">
        <% 10.times do |n| %>
          <tr>
            <% 10.times do |m| %>
              <td> <%= m*n %> </td>
            <% end %>
          </tr>
        <% end %>
      </table>
    </div>
  </div>

  <div class="row">
    <div class="bid col-md-4 col-md-offset-2">
      <p>your bid:{{current_bid}}<p>
      <form>
        <input></input>
      </form>
    </div>

    <div class="allbids col-md-4">
      <p>other users' bids</p>
      <ul>
        <li>user_1: 4</li>
        <li>user_3: 6</li>
        <li>user_2: 11</li>
      </ul>
    </div>

  </div>

  <div class="row">
    <div class="time-left col-md-offset-2">
      <p>Time Left: {{time_left}}</p>
    </div>
  </div>

  <div class="col-md-offset-2">
    <div id='moves' ng-controller='MovesController'>
      <div class="row">
        <h1>Moves</h1>
        <p>NewMove= {{newMove}}</p>
        <p>Number_of_moves= {{move.number_of_moves}}</p>
      </div>

      <svg height='200' width='200'>
        <circle cx='150' cy='150' fill='red' r='20'></circle>
      </svg>

      <h5>Seems that this code uses regular javascript in moves.js rather than our angular app. But, we use ng-submit, which doesn't get fired. And ng-model, which maybe does?</h2>
      <button class='another-move' ng-model='newMove.number_of_moves' ng-submit='saveMove()'>3</button>
      <button class='another-move minus' ng-model='newMove.number_of_moves' ng-submit='saveMove()'>-3</button>

      <h2>Whereas this uses our angular app</h2>

      <form ng-submit='saveMove()'>
        <input id='new-move' ng-model='newMove.number_of_moves' required='true' type='number'>
        <input type='submit' value='Save Move'>
      </form>
      <ul>
        <li ng-repeat='move in moves'>
          {{move.number_of_moves}}
        </li>
      </ul>

    </div>
  </div>

</div>
